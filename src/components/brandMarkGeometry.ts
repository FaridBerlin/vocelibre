/**
 * The bolt glyph, as one definition both brand marks draw from.
 *
 * These coordinates are the same path as src/assets/logo.svg and the bolt in
 * src/assets/icon-source-1024.png. They lived separately before, which is how
 * the in-app marks kept showing the retired circled sound-bars after the app
 * icons had already been rebranded — keep new surfaces pointed here rather
 * than pasting the path again.
 */
export const BRAND_MARK_VIEW_BOX = "0 0 1024 1024";

export const BRAND_MARK_BOLT_POINTS = "541,222 251,570 512,570 483,802 773,454 512,454";

/** Badge background: the rounded square behind the glyph, matching logo.svg. */
export const BRAND_MARK_BADGE_FILL = "#2056DF";
export const BRAND_MARK_BADGE_RADIUS = 241;
